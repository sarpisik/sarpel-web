import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(theme => ({
  link: { marginLeft: theme.spacing(5), fontWeight: 'bold' },
  icon: { marginRight: theme.spacing(0.5) }
}))
